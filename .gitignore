# Rust build artifacts
/target

# Cargo.lock is committed on purpose: this crate ships a binary, so pinned
# dependencies make `cargo install --locked` and CI builds reproducible.

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
nul

# Shell crash dumps (MSYS / Git Bash on Windows)
*.stackdump

# Test output
output.json
*.log

# Environment files
.env
.env.local
